.DS_Store
node_modules/
dist/
build/
.env
.env.local

# local SQLite data — never commit real research data
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# TS build cache
*.tsbuildinfo

# Claude Code local session settings (machine-specific, not shared)
.claude/settings.local.json

# Scratch JSON files passed between slash-command steps and their db-write
# helpers (extract-excerpts, comment-on-script) — ephemeral, never real data
.claude/tmp/

